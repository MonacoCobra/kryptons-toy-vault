# Figure archive backlog (legacy + metadata)

**Primary fill:** one-shot dump at `../figure-archive/oneshot.json` (see
`docs/figure-backlog.md`). This folder keeps the BBTS brand universe, rules,
manifest, and optional legacy `batch-NNN.json` files.

Batches (~180–250) are **optional legacy** only — not the growth cadence.

Release floor **1980**. Brand universe: `company-universe.txt` (BBTS A–Z).
Action figures only — no pins, dolls, statues, plush, or apparel.
